{
	"name": "@produck/mold-packages",
	"private": true,
	"scripts": {
		"deps:install": "npm -v && npm i && lerna bootstrap",
		"deps:clean": "lerna clean -y",
		"publish": "lerna publish --no-private"
	},
	"devDependencies": {
		"lerna": "^5.5.4"
	}
}
